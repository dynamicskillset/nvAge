---
description: Read git, kin, progress.md, and mistakes.md to get up to speed
agent: build
---

Read the project state so I can understand where things stand. Do the following:

1. Run `git log --oneline -15` to see recent commits.
2. Run `git status` to check for uncommitted changes.
3. Run `kin status` to see the semantic graph state.
4. Read `.opencode/progress.md` for the full project status.
5. Read `mistakes.md` if it exists, to learn from past failures.

Summarise: what the project is, what's been done recently, what's in progress, and any known issues or gotchas from the mistakes log.
